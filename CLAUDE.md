# 밥장부-직원용 (PoC)

## 프로젝트 개요
직원이 스마트폰으로 음식점 선금(충전)과 사용액을 기록하고, 음식점별 잔액·사용현황을 한눈에 보는 초간편 웹앱 PoC.
밥장부 본 시스템과는 독립된 단독 앱이며, 향후 연계 가능성만 데이터 구조로 대비한다.
상세 요구사항은 `밥장부-직원용_PoC계획서.md` 참조 — 구현 전 반드시 읽을 것.

## 기술 원칙
- **단일 index.html** (HTML+CSS+JS 모두 인라인, 프레임워크·빌드 도구 없음)
- 데이터는 **localStorage에만 저장** (서버·로그인·회원가입 없음)
- **스마트폰 우선 UI**: 큰 터치 버튼, 한 손 조작, 사용액 입력 3터치 이내
- 배포는 GitHub Pages 정적 호스팅을 목표로 함

## 핵심 요구사항 요약
- 최초 1회 이용 지역을 시·군·구 단위로 설정(예: 광진구). 인적사항은 일절 받지 않음
- 음식점 등록: **음식점명+동명 검색·선택이 기본**, 목록에 없으면 자유 입력 병행
- 충전 금액: **9,000원 배수 원터치 버튼**(9,000/18,000/27,000원…) + 직접 입력. 사용 금액 단위는 자유
- 홈(현황) 화면에 **자연어 입력창**: "강경불고기에 오늘 12000원 사용했어" → 음식점명·금액·날짜를 규칙 기반으로 파싱, 인식 결과를 확인시킨 뒤 저장
- 화면 구성: ⓪최초 설정(1회) ①홈(현황: 전체 잔액+음식점 카드) ②입력(사용/충전) ③내역·관리(수정·삭제·백업·복원)

## 음식점 실시간 검색 (공공데이터)
- 밥장부 중계 서버 프록시 `GET {RELAY_BASE}/api/restaurants?q=&region=` 사용(행안부 일반음식점 인허가, API 키는 서버 은닉, CORS는 nulmaru.github.io 허용됨)
- 광진구는 `REGION_CODES`의 개방자치단체코드(3040000)로 지역 한정 검색, 코드 실패·미등재 지역은 이름 검색 후 주소 필터(본체 앱과 동일 방식). 다른 지역 추가 시 `REGION_CODES`에 코드만 등재
- 검색어(가게 이름) 외 어떤 정보도 서버로 보내지 않는다. 오프라인·API 오류 시 내장 `SAMPLE_STORES` 폴백
- 검색 선택 등록 시 `restaurantId`(LOCALDATA 관리번호)를 저장 — 밥장부 본체와 같은 ID 체계(향후 연계 대비)

## 데이터 구조 (localStorage 키: bapjangbu_staff_v1)
- 설정: { region }
- 음식점: { id, name, dong, source(검색/자유입력), createdAt }
- 거래: { id, storeId, type(충전/사용), amount, date, memo, inputMethod(버튼/자연어) }

## 코딩 규약
- UI 문구·주석은 한국어, 금액은 천 단위 콤마 표시
- 잔액 = 충전 합계 − 사용 합계 (파생값, 저장하지 않고 계산)
- 큰 변경 전에는 계획을 먼저 제시하고 승인받을 것
